distributor:
  enable: true
  totalTaskGeneratorThreadNumber: 2
  totalTaskUpdaterThreadNumber: 1
  totalTaskDistributorPostThreadNumber: 8
  taskStoreMaxCount: 30
  timeoutMinutes: 5

interpolater:
  enable: true
  concurrentNumber: 8
  distributorURL: http://localhost:${server.port}
  callbackHttpEnum: http
  cellSize: 30

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ctpf2
    username: postgres
    password: w.w1w+
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  aop:
    auto: true